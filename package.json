{
  "name": "canal-seguro",
  "private": true,
  "description": "Canal Seguro — build/start a partir da raiz do repositório (Hostinger)",
  "scripts": {
    "build": "npm run build --prefix server",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
